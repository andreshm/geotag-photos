# GeoTag Photos dependencies
# Install with:  pip install -r requirements.txt
#
# NOTE: PySide6 bundles QtWebEngineWidgets — do NOT add PySide6-WebEngine
#       (that package does not exist on PyPI separately).

PySide6>=6.6.0
Pillow>=10.0.0
pyexiftool>=0.5.6
rawpy>=0.19.0
numpy>=1.26.0
